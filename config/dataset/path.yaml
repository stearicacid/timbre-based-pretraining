# Optional dataset variant for local path overrides.
#
# Usage:
#   python -m extract.main --config-name extract dataset=path dataset.data_dir=/abs/path/to/tfds

defaults:
  - nsynth

# Override only this field locally when needed.
data_dir: null